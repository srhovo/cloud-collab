# 阶段8P负责人当前一步操作卡

## 已确认

```text
https://app.xiaxue.site：真实浏览器正常访问，无证书警告，普通用户页面正常
https://admin.xiaxue.site：真实浏览器正常访问，无证书警告，管理员页面正常
域名、CNAME、HTTPS：已生效
```

## 现在只做三个只读隔离检查

依次在浏览器地址栏打开：

```text
https://app.xiaxue.site/__admin/
https://app.xiaxue.site/api/admin/session
https://admin.xiaxue.site/api/public/bootstrap
```

预期结果：

```text
1. app域名的/__admin/不能显示管理员控制台
2. app域名的/api/admin/session不能返回有效管理员会话或管理员能力
3. admin域名的/api/public/bootstrap不能返回普通端生产数据
```

允许出现的安全结果包括404、403、405、明确的关闭提示或不暴露数据的错误JSON。不要把错误页当作故障，也不要因此修改DNS。

请只回复三项各自显示的结果，或分别截图。截图不要包含密码、Cookie、Token、环境变量或浏览器开发者工具中的敏感请求头。

## 当前仍然禁止

```text
不填写生产环境变量
不生成或发送正式私密值
不执行Blob初始化execute
不启用生产功能开关
不晋升8.3.0
```

三个跨Host只读检查通过后，下一步才是导入同一EdgeOne项目的正式Origin和私密环境变量，并保持全部功能开关为0。
