# 阶段8O负责人当前一步操作卡

## 已确认

```text
EdgeOne项目：cloud-collab
项目数量：1
加速区域：全球可用区（不含中国大陆）
app.xiaxue.site：已生效
admin.xiaxue.site：已生效
app CNAME校验：已生效
admin CNAME校验：已生效
app HTTPS控制台状态：已配置
admin HTTPS控制台状态：已配置
```

## 现在只做一件事：零开关在线验收

分别在真实浏览器打开：

```text
https://app.xiaxue.site
https://admin.xiaxue.site
```

记录以下结果：

```text
1. 两个地址是否都没有证书警告
2. app地址是否显示普通用户页面
3. admin地址是否显示管理员页面或管理员关闭提示
4. app地址访问 /__admin/ 是否失败关闭
5. app地址访问 /api/admin/session 是否不能获得管理员能力
6. admin地址访问普通提交API时是否失败关闭
```

如果页面打不开，保留错误页面截图，不改DNS、不重复添加记录。

## 当前仍然禁止

```text
不填写生产环境变量
不执行Blob初始化execute
不启用任何生产功能开关
不把管理员密码发到聊天或提交GitHub
不晋升8.3.0
不修改xiaxue.site权威DNS服务器
不添加子域NS委派
```

控制台“已生效”与HTTPS“已配置”只证明平台配置已就绪；正式上线前仍需真实浏览器内容、证书和Host隔离验收。
