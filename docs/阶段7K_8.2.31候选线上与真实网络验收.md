# 阶段7K：8.2.31候选线上与真实网络验收

## 一、验收对象

- 候选版本：`8.2.31`
- 来源提交：`e0d648216e4be5e89774610681bb71d6741c4879`
- 候选字节数：`1,155,575`
- 候选SHA-256：`9a9719e70dce94d875befb287d247fca0755183da7c813779310abb57ba3882b`
- 发布通道：EdgeOne候选预览
- 验收日期：2026-07-21

为避免泄露或固化临时访问凭据，本文件不记录带`eo_token`的预览URL。

## 二、自动化证据

阶段7J三套工作流均成功：

- 候选打包工作流：成功；
- 主备入口发布预演：成功；
- 完整Node与浏览器矩阵：成功；
- Node测试：`301/301`；
- Cloud Functions相对导入审计：成功；
- 候选、EdgeOne预演和GitHub Pages预演HTML逐字节一致。

## 三、项目负责人现场验收

项目负责人通过截图和聊天确认以下项目均无问题：

- EdgeOne 8.2.31候选部署成功；
- `pages-release.json`版本、来源提交、候选摘要、字节数、稳定版边界正确；
- `build-manifest.json`版本、候选摘要、字节数、正式写入关闭状态正确；
- 页面显示`club`标签和`club_id`示例；
- 设备昵称示例显示为“例如：下雪”；
- club ID与library ID输入中文时保存被阻止；
- 管理员预览页面返回404，未进入公开白名单；
- iPhone Safari候选主页可正常使用；
- Wi-Fi和移动数据均可打开候选主页。

## 四、iOS直接查看JSON的中文显示

Safari阅读器直接展示两个公开JSON时，中文字符串仍可能显示为乱码；Android端显示正常。该现象仅影响人工直接阅读JSON中的中文字段，不影响：

- JSON结构和机器解析；
- 版本、摘要、字节数和布尔门禁；
- 候选主页中文显示；
- 本地码单、本地存储和导入导出；
- 协作绑定、ID校验或正式写入边界。

项目负责人明确接受其作为非阻断阅读器显示问题，本阶段不为此新增候选版本。

## 五、阶段结论

```text
8.2.31候选线上验收：通过
Wi-Fi访问：通过
移动数据访问：通过
iPhone Safari冒烟：通过
管理员页面隔离：通过
正式公共写入：关闭
稳定版：仍为8.2.25
稳定晋升授权：false
稳定晋升执行：false
```

阶段7K只固化验收证据，不修改候选HTML，不执行稳定晋升，不启用正式公共写入。
