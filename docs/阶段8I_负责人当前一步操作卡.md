# 阶段8M负责人当前一步操作卡

## 已确认

```text
域名：xiaxue.site
注册状态：正常
实名认证：已完成
注册日期：2026-07-21
到期日期：2027-07-21
自动续费：已开启
DNS服务商：DNSPod
DNS控制权：已确认
EdgeOne项目数量：1
app.xiaxue.site：已添加，部署中
admin.xiaxue.site：已添加，部署中
CNAME：尚未显示
HTTPS：尚未配置
```

## 现在只做一件事

```text
EdgeOne Makers
→ 当前唯一的 cloud-collab 项目
→ 域名管理
→ 刷新页面
```

等待以下两行都不再显示“部署中”，并且`CNAME`列出现具体目标值：

```text
app.xiaxue.site
admin.xiaxue.site
```

出现后，请发一张包含以下信息的截图：

```text
两个域名
两个状态
两个CNAME值
HTTPS配置状态
```

同时再补一张项目概览或项目设置中的“加速区域”截图。不要发送API Token、环境变量、项目密钥或带`eo_token`的预览链接。

## 当前不要做

```text
不要猜测CNAME
不要在DNSPod提前添加记录
不要申请HTTPS证书
不要购买轻量服务器或云服务器
不要启动ICP备案
不要购买付费DNS
不要购买单独SSL证书
不要创建第二个或独立管理员EdgeOne项目
不要填写生产环境变量
不要配置Blob初始化Secrets
不要运行真实初始化execute
```

EdgeOne官方流程是先完成域名添加和归属验证，再按控制台给出的精确值添加CNAME；解析生效后再配置免费HTTPS证书。首发目标仍为“全球可用区（不含中国大陆）”，当前不需要购买备案云资源。
