# 阶段8L负责人当前一步操作卡

域名证据已确认：

```text
域名：xiaxue.site
注册状态：正常
实名认证：已完成
注册日期：2026-07-21
到期日期：2027-07-21
自动续费：已开启
DNS服务商：DNSPod
DNS控制权：已确认
```

现在只做一件事：

```text
EdgeOne Makers
→ 项目列表
→ 打开当前唯一的 cloud-collab 项目
→ 项目概览或项目设置
→ 查看“加速区域”
```

目标值：

```text
全球可用区（不含中国大陆）
```

请发一张仅包含项目名称、关联仓库和加速区域的截图，或直接回复当前显示的加速区域。不要发送API Token、环境变量、项目密钥或预览令牌。

当前决定：

```text
首发区域：全球可用区（不含中国大陆）
当前ICP备案：不要求
当前云服务器购买：不需要
中国大陆节点：暂不启用
未来如需中国大陆节点：再单独办理ICP备案并准备符合条件的境内云资源
EdgeOne项目数量：1
正式域名：app.xiaxue.site、admin.xiaxue.site
```

当前不要做：

```text
不要购买轻量服务器或云服务器
不要启动ICP备案
不要购买付费DNS
不要购买单独SSL证书
不要提前添加CNAME/TXT
不要创建第二个或独立管理员EdgeOne项目
不要填写生产环境变量
不要配置Blob初始化Secrets
不要运行真实初始化execute
```

如果现有项目已经是“全球可用区（不含中国大陆）”，下一步就是在同一个项目添加两个自定义域名。如果不是该区域，先停止，不创建新项目，由阶段8L继续核对平台是否支持原项目调整或是否需要安全迁移。
