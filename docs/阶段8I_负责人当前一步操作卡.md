# 阶段8J负责人当前一步操作卡

现在只做一件事：

```text
腾讯云控制台
→ 域名注册
→ 我的域名
→ xiaxue.site
```

请核对页面显示：

```text
域名状态：正常
实名认证：已完成
到期时间：可见且正确
自动续费：已开启或明确自行续费
DNS服务器：可由本人控制
```

完成后保留一张不包含证件号码、手机号、邮箱完整值的截图，或直接回复上述五项状态。

当前不要做：

```text
不要购买轻量服务器
不要购买付费DNS
不要购买单独SSL证书
不要提前添加CNAME/TXT
不要创建第二个或独立管理员EdgeOne项目
不要填写生产环境变量
不要配置Blob初始化Secrets
不要运行真实初始化execute
```

代码侧已经把架构收敛为一个EdgeOne项目绑定两个自定义域名：

```text
app.xiaxue.site
admin.xiaxue.site
```

两者由全路由Host中间件严格隔离，并共享同一项目内的公共Blob与管理员Blob，不需要在长期运行环境中保存平台账户级API Token。下一步仍需先取得域名状态证据，再决定加速区域与备案路线。
