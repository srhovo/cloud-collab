# 阶段8I负责人当前一步操作卡

现在只做一件事：

```text
腾讯云控制台
→ 域名注册
→ 我的域名
→ xiaxue.site
```

请核对页面显示：

```text
域名状态：正常
实名认证：已完成
到期时间：可见且正确
自动续费：已开启或明确自行续费
DNS服务器：可由本人控制
```

完成后保留一张不包含证件号码、手机号、邮箱完整值的截图，或直接回复上述五项状态。

当前不要做：

```text
不要购买轻量服务器
不要购买付费DNS
不要购买单独SSL证书
不要提前添加CNAME/TXT
不要创建管理员EdgeOne项目
不要填写生产环境变量
不要配置Blob初始化Secrets
不要运行真实初始化execute
```

原因：普通入口和管理员入口计划已固定，但独立管理员项目跨项目访问公共Blob的安全架构仍在代码审计中。
