# 阶段8N负责人当前一步操作卡

## 已完成

```text
xiaxue.site注册、实名、自动续费、DNS控制权：已确认
EdgeOne项目：cloud-collab，运行中
加速区域：全球可用区（不含中国大陆）
项目数量：1
app.xiaxue.site：已生效，生产环境
admin.xiaxue.site：已生效，生产环境
根区域app CNAME：已录入
根区域admin CNAME：已录入
两个EdgeOne归属TXT：已录入
```

## 现在只做一件事

等待约10至30分钟后返回：

```text
EdgeOne Makers
→ cloud-collab
→ 域名管理
```

查看`app.xiaxue.site`和`admin.xiaxue.site`的CNAME/解析状态。两者都显示有效后，再发截图。

## 不要处理红色提示的方式

DNSPod中单独打开`app.xiaxue.site`或`admin.xiaxue.site`时出现“DNS服务器未生效”，是因为那两个独立子域解析区没有NS委派。不要修改主域名DNS服务器，不要添加子域NS记录。生产记录已经正确放在`xiaxue.site`根区域。

## 当前不要做

```text
不要重复添加CNAME或TXT
不要删除xiaxue.site根区域中的四条记录
不要修改域名DNS服务器
不要添加NS委派
不要配置环境变量
不要执行Blob初始化execute
不要启用生产开关
不要购买服务器、付费DNS或SSL证书
```

待CNAME状态验证后，下一步才是为两个域名依次申请并部署免费HTTPS证书。
