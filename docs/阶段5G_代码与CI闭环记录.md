# 阶段5G：普通共享类型代码与CI闭环记录

## 当前基线

- PR：#30
- base：`main@2ce00ae85564d36dcf2b97f8837cc40302f034e1`
- 通过完整CI的实现证据head：`5f0759667f5675993ff9bb12b879bb01032dc0c2`
- 后续仅允许追加证据记录；PR实时head以GitHub元数据为准
- 状态：Open / Draft / 未合并 / mergeable

## 已完成能力

1. `playable_name/upsert`
   - 仅上传已被用户明确确认的陪玩名字；
   - NFKC与空白规范化；
   - 团级业务键与内容Hash；
   - 联系方式、链接、邮箱、电话、控制字符和超长内容失败关闭；
   - 两台独立设备确认或可信设备可进入批准条件。

2. `boss_profile/upsert`
   - 公开字段严格限制为`bossName / paiDan / discount`；
   - 稳定派生`bossId`，不上传本地老板键；
   - 新老板资料按普通共享候选处理；
   - 已有老板只有直属不变且折数在0.8至1范围内合理下降时进入普通路径；
   - 直属变化、折数升高和其他敏感变化标记为阶段6处理，不产生公共变更。

3. 多类型公共事件与同步
   - 保持`schemaVersion: 1`；
   - `exact_price`继续委托冻结协议实现，字节语义兼容；
   - 名字与老板资料进入不可变公共事件、增量读取和快照；
   - 客户端执行价格、已确认名字和老板资料的混合三方合并；
   - 本地修改与远端变化冲突时保留本地并记录冲突；
   - 普通类型删除标记当前失败关闭。

4. 管理员审核
   - 独立队列、详情、批准、拒绝和修改后批准路由；
   - 复用阶段5A管理员会话与同源HTTPS校验；
   - 不返回原始设备ID、存储Key、请求Hash等内部身份；
   - 阶段6敏感原因在页面和服务端同时锁定。

5. 客户端候选生成
   - 仅参与协作绑定且固定合成作用域可入队；
   - 初次绑定只投影已确认名字和老板公开字段；
   - 用户明确确认名字、保存老板资料或保存最近老板时才生成候选；
   - import、migration、cloudPull、rollback、system以及私人数据不会入队；
   - 正式公共写入、自动批准和阶段6敏感能力继续关闭。

## CI闭环

GitHub Actions：`#842`，run id `29710657218`，结论`success`。

- Node测试：200 / 200通过；
- Stage4C兼容与隐私门禁：60 / 60通过；
- Stage5F回归门禁：17 / 17通过；
- Stage5G静态、隐私与兼容门禁：23 / 23通过；
- 冻结核心对比通过；
- 普通浏览器集成通过；
- Stage5A、5B、5C、5D、5E、5F、5G浏览器回归全部通过。

构建产物SHA-256：

```text
377a889f4fc0eef019c992adfb18e6be921729e987f3f7e692cbc0ca553e130f
```

## 尚未执行

- 未将PR标记为Ready；
- 未合并到main；
- 未创建EdgeOne项目、部署、环境变量、设备或Blob对象；
- 未开放阶段6敏感规则；
- 未修改码单器8.2.25稳定文件。

未经明确授权，PR #30继续保持Draft和未合并状态。
