{
  "name": "mdq-cloud-collab-preview-write-stage4b2",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@edgeone/pages-blob": "0.0.14"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate:base": "node scripts/validate.mjs",
    "validate:cleanup": "node scripts/validate-stage4c-cleanup.mjs",
    "validate": "npm run validate:base && npm run validate:cleanup",
    "ci": "npm run build && npm test && npm run validate"
  }
}
