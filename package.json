{
  "name": "mdq-cloud-collab-submission-stage4a",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@edgeone/pages-blob": "0.0.14"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "ci": "npm run build && npm test && npm run validate"
  }
}
