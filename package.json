{
  "name": "mdq-cloud-collab-submission-stage4b",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "ci": "npm run build && npm test && npm run validate"
  },
  "dependencies": {
    "@edgeone/pages-blob": "latest"
  }
}
