{
  "name": "mdq-cloud-collab-routing-stage4b",
  "version": "0.6.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "ci": "npm run build && npm test && npm run validate"
  },
  "dependencies": {
    "@edgeone/pages-blob": "0.0.14"
  }
}
