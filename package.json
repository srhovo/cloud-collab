{
  "name": "mdq-cloud-collab-preview-write-stage4b2",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@edgeone/pages-blob": "0.0.14"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs && node scripts/validate-stage5f.mjs",
    "ci": "npm run build && npm test && npm run validate"
  }
}
